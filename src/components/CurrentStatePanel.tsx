"use client";
import { useEffect,useMemo,useState } from "react";
import type { DailyLifeRecord,DailyLifeScheduleEntry } from "../lib/types";
function clock(){const p=new Intl.DateTimeFormat("en-GB",{timeZone:"Europe/Istanbul",hour:"2-digit",minute:"2-digit",hour12:false}).formatToParts(new Date());const h=Number(p.find(x=>x.type==="hour")?.value??0),m=Number(p.find(x=>x.type==="minute")?.value??0);return{time:`${String(h).padStart(2,"0")}:${String(m).padStart(2,"0")}`,minutes:h*60+m}}
function mins(e:DailyLifeScheduleEntry){const[h,m]=e.time.split(":").map(Number);return h*60+m}
export function CurrentStatePanel({dailyLife}:{dailyLife:DailyLifeRecord}){const[c,setC]=useState({time:"--:--",minutes:0});useEffect(()=>{setC(clock());const t=window.setInterval(()=>setC(clock()),60000);return()=>window.clearInterval(t)},[]);const current=useMemo(()=>[...dailyLife.schedule].sort((a,b)=>mins(a)-mins(b)).filter(e=>mins(e)<=c.minutes).at(-1)??dailyLife.schedule[0],[c.minutes,dailyLife.schedule]);return <section className="consciousness-module current-state-module home-live-state"><div className="consciousness-heading"><h2>ucu beden şu an</h2><span>{c.time} / İstanbul</span></div><p className="current-activity">{current?.activity??dailyLife.activity}</p><dl className="state-list"><div><dt>ruh hali</dt><dd>{dailyLife.mood}</dd></div><div><dt>enerji</dt><dd>{dailyLife.energy<.35?"dipte":dailyLife.energy<.65?"ölçülü":"açık"}</dd></div><div><dt>takıntısı</dt><dd>{dailyLife.obsession}</dd></div><div><dt>kaçındığı</dt><dd>{dailyLife.avoidance}</dd></div></dl><p className="inner-note">“{current?.inner_note??dailyLife.inner_note}”</p></section>}
